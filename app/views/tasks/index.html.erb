<div>
<h1>My Tasks</h1>
<ul id="tasks">
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
        <i class="fa fa-check-square-o"></i>
      <% else %>
        <i class="fa fa-square-o"></i>
      <% end %>
      <%= link_to task.title, tasks_path(task.id) %>
      ·
      <%= link_to tasks_path(task.id), method: :delete, data: { confirm: "Delete #{task.title}?" } do %>
        <i class="fa fa-trash-o" aria-hidden="true"></i>
      <% end %>
    </li>
  <% end %>
</ul>
</div>
<%= link_to tasks_new_path do %>
  <i class="fa fa-plus"></i> Add a new task
<% end %>
